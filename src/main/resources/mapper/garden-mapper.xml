<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.korit.florographyapi.garden.mapper.GardenMapper">


    <insert id="insertGardenData" parameterType="com.korit.florographyapi.garden.dto.GardenCreateRequest">
        insert into garden_data values (default, #{userId}, #{gardenName}, #{gardenData}, #{gardenImage}, #{createdAt}, now())
    </insert>

    <select id="selectAllGardens" resultType="com.korit.florographyapi.entity.Garden">
        select id, user_id as userId, name, garden_data as garden_data, garden_image as gardenImage, created_at as createdAt, updated_at as updatedAt
        from garden_data
        where user_id = #{userId}
        order by created_at desc
    </select>

    <select id="selectGardenById" parameterType="Long" resultType="com.korit.florographyapi.entity.Garden">
        select id, user_id as userId, name, garden_data as garden_data, garden_image as gardenImage, created_at as createdAt, updated_at as updatedAt
        from garden_data
        where id = #{id}
    </select>

</mapper>